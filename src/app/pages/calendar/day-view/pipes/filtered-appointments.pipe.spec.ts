import { FilteredAppointmentsPipe } from './filtered-appointments.pipe';

describe('FilteredAppointmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredAppointmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
