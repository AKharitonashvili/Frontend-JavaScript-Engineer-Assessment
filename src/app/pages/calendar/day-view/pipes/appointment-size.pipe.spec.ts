import { AppointmentSizePipe } from './appointment-size.pipe';

describe('AppointmentSizePipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentSizePipe();
    expect(pipe).toBeTruthy();
  });
});
